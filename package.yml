name       : ruby-yard
version    : 0.9.9
release    : 2
source     :
    - https://rubygems.org/downloads/yard-0.9.9.gem : aae74f34bb1de444d5429a213ef995738d9d3a0fe5cc7ae061756277bc02a6a7
license    : MIT
component  : programming
summary    : YARD is a documentation generation tool for the Ruby programming language.
description: |
    YARD is a documentation generation tool for the Ruby programming language.
extract    : no
builddeps  :
    - pkgconfig(ruby-2.2)
rundeps    :
    - ruby
install    : |
    export geminstalldir=$(ruby -e'puts Gem.default_dir')
    export GEM_HOME=$geminstalldir
    export GEM_PATH=$geminstalldir
    gem install \
        --ignore-dependencies \
        --no-user-install \
        -i "$installdir/$geminstalldir" \
        -n "$installdir/usr/bin" \
        $sources/yard-$version.gem
